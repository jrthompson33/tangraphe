/**
 * Created by John Thompson
 */

(function () {

    // Global object that can be called by other methods
    tool = {};

    /**
     * Initialize the tool
     * @param params - the context of the visualization
     */
    tool.init = function (params) {

    };

    tool.draw = function (params) {

    };

})();